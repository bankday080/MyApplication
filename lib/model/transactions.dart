class Transactions {
  String name; //ชื่อ
  String farm_name; //ขื่อฟาร์ม
  DateTime user_name; //ชื่อผู้ใช้

  Transactions(
      {required this.name, required this.farm_name, required this.user_name});
}
